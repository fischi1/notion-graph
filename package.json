{
  "scripts": {
    "dev-watch": "nodemon --watch 'src/' --ext 'ts' --exec npm run dev",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "build": "tsc -p ."
  },
  "dependencies": {
    "@types/node": "^17.0.12",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}

import Store from "./components/Store"
import Routing from "./Routing"

const UserInterface = () => {
    return (
        <Store>
            <Routing />
        </Store>
    )
}

export default UserInterface
